# 医疗公告分析 Agent：问题与应对记录

> 用途：持续记录项目学习、架构设计、简历包装和面试准备过程中提出的问题，以及对应结论、应对方案和面试表述。
>
> 维护规则：
>
> 1. 历史问题编号保持不变；新增问题继续递增。
> 2. 答案发生变化时，在原问题下增加“更新记录”，不静默删除旧结论。
> 3. 每次新增问题后，同时检查是否需要更新项目包装总上下文。
> 4. 本文件记录“为什么这样设计”和“面对追问如何回答”；最终统一口径以 `medical-notice-analyzer/docs/career/PROJECT_PACKAGING_CONTEXT.md` 为准。
> 5. 字段名、配置名、指标名或组件名首次出现时，必须同时解释：中文含义、数据来源、存在原因、读写节点和脱敏示例。
>
> 下一条问题编号：**23**

## 相关文档

- `medical-notice-analyzer/docs/career/PROJECT_PACKAGING_CONTEXT.md`：跨对话唯一入口，保存当前最终包装口径。
- `docs/career/medical-notice-analyzer-dataflow-learning-notes.md`：逐步数据格式、字段和基础技术学习笔记。
- `docs/career/medical-notice-analyzer-resume-packaging.md`：简历项目描述和面试叙事。

## 已确认的项目事实

- 业务以带量采购、集中采购及其接续类公告为主。
- 公司上游数据平台每天采集约9000～10000条公告；本项目不负责公告采集，只读取用户选择或公司GraphRAG召回的资料进行分析。
- 内部验证阶段正式进入AI分析链路的任务通常每天不超过10份，这是历史业务事实。
- 客户化包装完成态按日均300～500份、活动期峰值约1000份/日进行容量设计；这些数字是目标容量和模拟口径。
- 主材料由用户显式选择1～3条项目公告；辅助材料可手动选择0～10条，未手选时由公司GraphRAG自动召回1～10条。
- 公司GraphRAG在包装完成态支持关键词、向量、知识图谱、结构化过滤和Rerank。
- 单份正式报告通常为2000～5000字，至少包含4～5个章节。
- 报告生成使用`deepseek-v4-pro`；质检、修复与反馈修订使用`deepseek-v4-flash`；DeepEval Judge使用公司部署的GLM-5与DeepSeek V4 Flash。
- 自动修复最多2次；用户自助反馈修订最多3轮，二者分别计数。
- 确定性解析不等于关键词截取：文件结构解析、候选识别、字段映射、值标准化和一致性校验属于不同层级。
- 当前仓库已有用户文字反馈后的报告修订能力；GraphRAG、统一LangGraph、弹性Celery和完整偏好记忆属于包装完成态。

---

## 目录

1. [Evidence Pack 是否需要像 RAG 一样提前离线处理](#1-evidence-pack-是否需要像-rag-一样提前离线处理)
2. [专用解析器与 Apache Tika、MarkItDown 如何选择](#2-专用解析器与-apache-tikamarkitdown-如何选择)
3. [重要信息未进入证据包时如何发现和兜底](#3-重要信息未进入证据包时如何发现和兜底)
4. [现有证据包构建方法是否合理](#4-现有证据包构建方法是否合理)
5. [LangChain、LangGraph 和 HTTP 调用的上下文限制](#5-langchainlanggraph-和-http-调用的上下文限制)
6. [完整数据流第 0～11 步分别存在哪里](#6-完整数据流第-011-步分别存在哪里)
7. [DeepEval 指标是否足够以及报告应如何全面评估](#7-deepeval-指标是否足够以及报告应如何全面评估)
8. [用户反馈修订和公司内部资料 RAG 如何设计](#8-用户反馈修订和公司内部资料-rag-如何设计)
9. [出现字段名时应如何解释](#9-出现字段名时应如何解释)
10. [基础证据包与 LLM 压缩是否会造成信息损失](#10-基础证据包与-llm-压缩是否会造成信息损失)
11. [A 类证据与 B 类证据是否冗余](#11-a-类证据与-b-类证据是否冗余)
12. [完整链路每一步的数据格式是什么](#12-完整链路每一步的数据格式是什么)
13. [哪些指标评估报告、模型、检索和系统](#13-哪些指标评估报告模型检索和系统)
14. [Agent State 是什么以及为什么需要它](#14-agent-state-是什么以及为什么需要它)
15. [Celery 的基础知识和项目中的定位](#15-celery-的基础知识和项目中的定位)
16. [通过共享 RAG 将内部工具升级为客户产品是否合理](#16-通过共享-rag-将内部工具升级为客户产品是否合理)
17. [公司统一付费知识库应该包含哪些类型](#17-公司统一付费知识库应该包含哪些类型)
18. [跨对话文档如何分工并保持同步](#18-跨对话文档如何分工并保持同步)
19. [客户化后每日分析任务量应该如何调整](#19-客户化后每日分析任务量应该如何调整)
20. [用户总结的完整数据流是否正确以及如何补齐工程机制](#20-用户总结的完整数据流是否正确以及如何补齐工程机制)
21. [确定性解析器是否只是关键词提取以及如何识别模糊语义](#21-确定性解析器是否只是关键词提取以及如何识别模糊语义)
22. [最终九步包装流程如何确定并转换成简历3+15](#22-最终九步包装流程如何确定并转换成简历315)

---

# 1. Evidence Pack 是否需要像 RAG 一样提前离线处理

## 问题

日采集约一万条公告，是否应该全部提前完成附件下载、OCR、表格解析和 Evidence Pack 构建，让用户点击后直接生成报告？用户选择材料后再处理，还算不算预处理？

## 结论

采用：

> **全量轻处理 + 热点预处理 + 用户选择后按需深处理 + 内容哈希缓存复用。**

- 全量轻处理：所有公告做元数据清洗、正文去噪、内容哈希、文件类型识别、去重和业务分类。
- 文档资产预处理：对高价值或高频材料提前下载附件并解析，形成可复用`ParsedDocument`。
- 任务级Evidence Pack组装：用户选择主材料、辅助材料和分析目标后，再组装本次任务证据包。

## 面试表述

> 我没有对每天近万条公告执行同等成本的深度解析，而是将离线文档资产处理与在线任务证据组装拆开。全量数据只做低成本清洗和分类，高价值公告提前解析，其他材料按需处理，并通过`source_hash`和`parser_version`复用缓存。

## 更新记录：2026-08-04

- 每天约9000～10000条公告的采集和基础入库属于公司上游数据平台，不属于本项目负责范围。
- 本项目只消费公告库数据，负责用户选材、GraphRAG辅助召回、深度解析、证据包构建和报告分析。
- 简历不得写成“本项目日采集9000～10000条公告”，可以回答为“平台可使用公司公告库中的数据”。

---

# 2. 专用解析器与 Apache Tika、MarkItDown 如何选择

## 结论

> **统一解析接口 + 关键格式专用适配器 + Tika/MarkItDown通用兜底。**

```text
PDF   -> pypdf等文本提取 -> 低文本密度时OCR -> 通用解析器兜底
DOCX  -> python-docx -> 通用解析器兜底
DOC   -> LibreOffice受控转换 -> DOCX解析 -> 通用解析器兜底
XLSX/XLSM -> openpyxl(read_only/data_only，不执行宏) -> 通用解析器兜底
XLS   -> xlrd或受控转换 -> 通用解析器兜底
未知格式 -> Tika或MarkItDown
```

| 字段 | 含义 | 为什么需要 |
|---|---|---|
| `parser_name` | 实际解析器 | 比较解析质量和定位故障 |
| `parser_version` | 解析规则版本 | 判断缓存是否失效 |
| `fallback_used` | 是否进入兜底 | 统计主解析器失败率 |
| `parse_status` | 成功、部分成功或失败 | 决定是否生成或转人工 |

安全要求：文件魔数和扩展名双校验、大小/页数/行数上限、压缩包防路径穿越、宏不执行、外链不自动访问、解析进程资源隔离。

---

# 3. 重要信息未进入证据包时如何发现和兜底

如果信息没有进入完整Evidence Pack，也没有进入模型输入，模型不应该可靠覆盖它。质量评估必须覆盖：

```text
Raw Source -> Parsed Asset -> Full Evidence Pack -> Compact Payload -> Report
```

发现必需主题缺失时，回到完整解析产物定向检索，必要时使用受约束Fact Extractor补建带`source_ref`的新A/B证据；仍找不到则`needs_manual_review=true`。

---

# 4. 现有证据包构建方法是否合理

当前方案作为受控领域工程原型合理，优点包括主辅材料分离、专用解析、Full Pack与Compact Payload分离、A/B/C分层、Mandatory Evidence保护、解析告警和Claim-Evidence质量门。主要不足是复杂文档解析边界、规则Recall上限和缺少系统Source-to-Evidence标注评测集。

---

# 5. LangChain、LangGraph 和 HTTP 调用的上下文限制

LangGraph负责State、Node、Edge和执行路由；LangChain提供统一模型调用接口；HTTP负责传输。它们都不会自动替应用控制上下文。

| 字段 | 含义 |
|---|---|
| `model_context_tokens` | 模型总上下文窗口 |
| `estimated_input_tokens` | 本次输入Token估算 |
| `reserved_output_tokens` | 为报告输出预留Token |
| `safety_margin_tokens` | 工具消息和估算误差余量 |
| `application_input_cap_chars` | 应用字符硬上限，例如240000 |

应用最终输入必须同时满足字符硬上限和Token预算。

---

# 6. 完整数据流第 0～11 步分别存在哪里

- MySQL：公告、客户任务、状态、版本、关系、反馈、偏好候选和指标汇总。
- MinIO：原始附件、解析产物、Full Evidence Pack、Compact Payload、ReportIR、Markdown、DOCX/PDF。
- Redis：Celery Broker、短期缓存、进度和锁，不作为永久事实源。
- LangGraph State：小型状态与对象引用。
- Celery消息：只传`run_id`、`pack_id`等标识。

---

# 7. DeepEval 指标是否足够以及报告应如何全面评估

当前四项DeepEval是最小Advisory版本。完整体系分为解析、Evidence、GraphRAG、报告、修订、工程和业务六层。DeepEval异步旁路，不阻塞正式报告交付。

---

# 8. 用户反馈修订和公司内部资料 RAG 如何设计

## 原结论

反馈分为样式/结构修改、当前公告补证、共享知识扩展。共享知识扩展需要构建`revision_evidence_pack`，不能把历史项目事实覆盖为当前项目事实。

## 更新记录：2026-08-04

- 包装完成态采用公司GraphRAG，组合关键词、向量、知识图谱、结构化过滤和Rerank。
- 纯样式反馈也必须携带原Evidence Pack或受限事实引用，不能只把“报告+反馈”交给模型。
- 用户自助修订最多3轮，由`user_revision_count`计数；超过3轮或仍有阻断问题转人工。
- 网络重试、Celery重复投递和同一轮模型重试不计入用户修订次数。
- 用户反馈日志与偏好记忆分开；只有稳定样式偏好可以形成候选记忆，且必须用户确认后生效。

---

# 9. 出现字段名时应如何解释

固定模板：字段/组件名、中文含义、数据来源、存在原因、读写节点、脱敏示例。

示例：`run_id`是一次分析运行唯一标识，由任务创建接口生成，关联MySQL、LangGraph、Celery、日志、报告版本和DeepEval结果。

---

# 10. 基础证据包与 LLM 压缩是否会造成信息损失

## 原结论

不能承诺100%无损。目标是原始资料可追溯、关键事实强制保真、压缩损失可量化、异常可转人工。

## 更新记录：2026-08-04

- `application_input_cap_chars=240000`只表示应用硬上限，不表示应把模型输入压到接近24万字符。
- 必须为系统提示词、当前任务、偏好上下文、工具消息、输出Token和估算误差预留空间。
- 最终以Token估算校验为准。
- LLM压缩失败时使用规则压缩；仍超限则分阶段生成。
- Mandatory Evidence不能保留时，任务不得强行生成，进入人工复核。

---

# 11. A 类证据与 B 类证据是否冗余

A类保存“原文是什么、在哪里”；B类保存“程序如何标准化理解原文”。B类必须记录`derived_from`、`extractor_version`、`normalized_value`和`source_hash`。两者是有意冗余，不是两份独立真相。

---

# 12. 完整链路每一步的数据格式是什么

```text
MySQL公告记录 + 附件元数据
 -> Material
 -> ParsedDocument
 -> GraphRAGSelection
 -> Full Evidence Pack
 -> FactItems + AnalysisItems
 -> Compact Payload
 -> ReportIR + Markdown
 -> QAResult / Claim-Evidence Index
 -> Repaired Report Version
 -> Final Analysis Run + DOCX
 -> DeepEval Projection + Metric Result
```

---

# 13. 哪些指标评估报告、模型、检索和系统

- 报告：Claim Support、Critical Coverage、Unsupported Claim、数值日期Exact Match、结构完整性。
- GraphRAG：Contextual Precision/Recall/Relevancy、Top-K Hit Rate、MRR、图关系命中率、过期资料召回率。
- 修订：反馈遵循度、事实回归率、修改局部性、一次修订通过率。
- 工程：API/队列/端到端P95、成功率、超时率、重试率、并发、积压和成本。

---

# 14. Agent State 是什么以及为什么需要它

Agent State是节点间共享的数据快照，不是状态机本身。State、Node、Edge和Checkpoint共同组成有状态工作流。State只保存小型状态和对象引用，大对象进入MinIO。

---

# 15. Celery 的基础知识和项目中的定位

Celery负责任务何时执行、在哪个Worker执行、队列路由、超时和重试；LangGraph负责单次任务内部节点和条件边。客户化后拆分附件、生成、质检、修订、导出和评测队列，按积压和等待P95扩缩容Worker。

---

# 16. 通过共享 RAG 将内部工具升级为客户产品是否合理

方向合理。所有客户使用共享公开公告库和公司审核知识库，不支持客户私有文件或私有知识库。账户级样式偏好不等于客户私有事实知识库。

---

# 17. 公司统一付费知识库应该包含哪些类型

推荐集采项目时间线、政策规则、地区准入规则、历史价格采购量、企业产品主数据、公司审核分析、风险规则案例、术语本体、专家问答和数据质量修复。首期优先前五类核心知识。

---

# 18. 跨对话文档如何分工并保持同步

问题日志记录为什么，总上下文记录最终结论，学习笔记记录字段与数据流，简历文档记录对外表达。总上下文冲突时优先，但必须修复详细文档。

---

# 19. 客户化后每日分析任务量应该如何调整

保留内部每天10份以内为历史事实；客户化完成态按日均300～500份、活动期峰值约1000份/日设计。模拟容量还包括50份/小时、稳定并发20、队列等待P95 60秒、常规任务P95 180秒、最终成功率98.8%。这些不是当前真实生产数据。

---

# 20. 用户总结的完整数据流是否正确以及如何补齐工程机制

## 问题

用户按照主辅材料、证据构建与压缩、Prompt生成、质检、修订、二次质检、后端校验、Word导出、用户反馈和记忆沉淀总结了完整链路。哪些内容正确，哪些与既有包装冲突，怎样补齐每一步的兜底、重试、超时、安全和并发机制？

## 总体判断

整体方向约**75%正确**，主链路顺序基本合理，但有六个必须修正的关键点：

1. 辅助材料不只限于“项目公告类型”，还包括政策、历史轮次、地区规则、公司审核分析和风险规则。
2. GraphRAG返回的是候选资料，不是可直接进入报告的事实；候选资料仍需解析并构建A/B证据。
3. C级证据不是推理结论。推理、风险判断和建议属于独立Analysis Layer。
4. 24万字符是应用硬上限，不应将输入尽量压满；必须预留Prompt、输出Token和安全余量。
5. 后端超时、重试、Watchdog和并发控制贯穿每个节点，不是二次质检之后才执行。
6. 用户反馈不能只用“当前报告+意见”修订；至少要带原Evidence Pack。长期记忆必须经过候选提取和用户确认。

## 第0步：鉴权、限流与任务创建

用户总结中缺少该步骤。客户请求进入FastAPI后执行鉴权、账号额度、令牌桶限流、参数Schema、材料数量、幂等和权限检查；随后在MySQL事务创建`run_id`、材料关系和Outbox事件，再由Celery异步执行。

- 输入：`customer_id`、`requested_by_user_id`、`idempotency_key`、分析目标、材料ID。
- 输出：`run_id`、`status=queued`。
- 存储：MySQL任务表和Outbox；Redis只接收后续队列消息。
- 兜底：Redis不可用时Outbox保留，恢复后补投。
- 重试：客户端重试使用相同幂等键，返回原`run_id`。
- 超时：API只完成接单，不等待模型；目标提交P95小于200ms属于模拟指标。
- 安全：客户只能选择系统已有资料；服务端重新校验ID和范围。
- 并发：单客户和全局双层限流，防止单客户占满队列。

## 第1步：主材料与GraphRAG辅助材料

- 主材料：用户显式选择1～3条项目公告。
- 辅助材料：0～10条，可手动选择，也可由GraphRAG自动召回。
- GraphRAG：关键词、向量、知识图谱关系、地区/品类/阶段/时间/`source_scope`过滤、Rerank、去重和有效期过滤。
- 默认可先召回更大的候选池，再选取1～10条最终材料；不能把初始Top-K直接等同于最终证据。

兜底与工程机制：

- GraphRAG不可用：降级为关键词+结构化过滤；仍不可用时仅使用用户手动选材并显示召回降级告警。
- 超时：查询理解、三路召回和Rerank分别设置超时，总检索超时后返回已完成的高置信候选。
- 重试：只对网络、服务繁忙和短时存储故障重试；空召回不重复轰炸服务。
- 安全：先做权限与`source_scope`过滤，再将内容返回任务；过期规则按`effective_date`过滤。
- 并发：关键词、向量和图关系可以并行召回，但使用独立并发池和超时预算。

## 第2步：正文清洗、附件解析和ParsedDocument缓存

用户说“正文已经是MySQL中的文本文档”不够准确。MySQL通常保存HTML正文和元数据，系统清洗成`content_text`。附件解析完成态采用：PDF文本提取后按低文本密度触发OCR；DOCX用`python-docx`；XLSX/XLSM用`openpyxl`做结构、统计和有限样例；DOC/XLS使用受控转换或通用解析器兜底。

- 输入：MySQL公告记录、附件元数据、MinIO原始附件。
- 输出：`ParsedDocument`，包含文本、表格、页码/行列定位、状态、告警和哈希。
- 存储：大解析产物在MinIO；MySQL保存`parser_name/version`、状态、对象Key和哈希。
- 缓存：`source_hash + parser_version`相同则复用。
- 兜底：主解析器→OCR/受控转换→Tika或MarkItDown→部分成功/人工复核。
- 重试：下载失败、对象存储短时失败可重试；文件损坏、密码保护和不支持格式不盲目重试。
- 超时：下载、OCR、每文件解析和整组附件分别超时。
- 安全：文件魔数、大小、页数、行数、压缩比检查；XLSM不执行宏；ZIP防路径穿越；解析进程限制CPU/内存。
- 并发：不同附件可并行；相同附件用分布式锁避免重复OCR。

## 第3步：Full Evidence Pack、Fact Layer与Analysis Layer

Evidence Pack主要由确定性解析、业务规则、词典、正则和结构化提取器构建。“主要靠正则”表述过窄。复杂表头或语义可用受约束LLM生成候选，但必须校验。

- A级：原文、表格单元格等直接证据，包含`source_ref`。
- B级：A的标准化事实，包含`normalized_value`、`derived_from`、`extractor_version`和`source_hash`。
- C级：摘要、生成指导、告警、诊断和已确认的样式偏好等辅助内容，不能独立支撑事实。
- Analysis Layer：推理、风险和建议，引用`supporting_fact_ids`并记录置信度、风险和不确定性。

正确示例：

```json
{
  "fact_id": "fact_deadline_001",
  "field": "procurement_start_date",
  "raw_value": "5月20日",
  "normalized_value": null,
  "evidence_ids": ["ev_a_001"],
  "uncertainty": "原文未明确年份"
}
```

```json
{
  "analysis_id": "analysis_prepare_001",
  "conclusion": "企业应在项目开始前完成材料准备",
  "supporting_fact_ids": ["fact_deadline_001"],
  "confidence": 0.78,
  "risk_level": "medium",
  "uncertainty": "未掌握企业当前准备进度"
}
```

不能在原文没有年份时擅自标准化出具体年份。

兜底：规则未识别→受约束LLM候选→Schema与来源校验→仍不确定则保留未知并转人工。冲突事实不能自动任选一个。

## 第4步：Compact Payload二次压缩

24万字符作为`application_input_cap_chars`硬上限可以接受，但不能压到接近24万作为实际输入目标。需要同时维护Token预算。

- 先去重、删除模板噪声、保留Mandatory Evidence。
- 再进行规则压缩；仍过长时使用LLM分块压缩。
- 最后估算Token，扣除系统Prompt、任务Prompt、偏好、工具消息、输出Token和安全余量。
- 仍超限则使用`staged_generation`，按章节或材料分阶段生成。

兜底：LLM压缩失败→规则压缩；Mandatory Evidence无法100%保留→不强行生成，转人工。

## 第5步：Prompt组装与报告生成

用户提出的系统Prompt、当前任务Prompt和用户偏好三部分方向正确，但应增加Evidence Context，并明确优先级：

1. 系统规则与输出Schema；
2. 当前任务指令；
3. Compact Payload与来源引用；
4. 用户已确认的结构化偏好Profile。

偏好只能控制长度、表格比例、重点顺序和表达风格，不能修改事实。公告正文、附件、RAG资料和用户反馈都视为不可信数据，用分隔符包裹，防止提示词注入。

生成节点使用DeepSeek V4 Pro，输出ReportIR和Markdown。模型调用设置连接/读取/总超时、并发信号量、Token上限、Schema校验和版本记录。限流或网络失败指数退避；Schema失败可进行一次受控格式修复，不能无限重试。

## 第6步：第一次质量检查

质检不只是检查发布机构、时间和标的物，还包括：

- ReportIR Schema和非空输出；
- 必需章节与关键主题；
- 数值/日期Exact Match；
- 附件状态一致性；
- Claim-Evidence支持和来源定位；
- C级被误当事实；
- 历史项目事实泄漏；
- 禁用表达与高风险无证据结论。

规则质量门和DeepSeek V4 Flash语义质检输出统一`QAResult`，状态为`passed`、`needs_fix`或`blocked`。DeepEval不属于同步阻断门，保持异步旁路。

## 第7步：定向修复

方向正确，但不是把问题段落无限交给模型重写。

1. 先做确定性修复：结构、空字段、无证据表格行、禁用表达。
2. 再把目标段落、问题码、允许使用的A/B证据和禁止新增事实约束交给DeepSeek V4 Flash。
3. 生成新ReportIR版本，并记录`repair_actions`和前后diff。
4. `auto_repair_count`最多2次。

并发安全：报告版本使用乐观锁；两个修复任务不能覆盖同一基线版本。

## 第8步：二次质检、最终持久化与交付判断

二次质检使用与第一次相同的门禁。通过后执行最终后端校验：ReportIR/Markdown一致、对象哈希、版本关系、MySQL/MinIO持久化、状态迁移和`deliverable`。如果仍存在高风险无证据事实、关键附件失败或阻断问题，则不能作为正式可下载报告交付，转人工。

Watchdog、超时和重试贯穿每个节点；最终步骤只做状态收口和一致性校验。

前端立即展示确定性指标，例如A/B支持率、无证据断言数、缺失主题、告警和可交付状态。DeepEval显示`pending`，异步完成后再更新评分；Judge不可用不阻塞报告。

## 第9步：Word导出

只有`deliverable=true`的版本允许正式导出。DOCX由ReportIR受控渲染，文件名清洗、模板和图片白名单、短时签名下载链接。导出失败单独重试，不重新生成报告。

## 第10步：用户反馈与补证修订

预制按钮和自由文本都先进入统一反馈记录。不能因为是预制按钮就只把“意见+最终报告”交给模型，至少需要原Evidence Pack。

- 样式/结构：当前报告+反馈+原Evidence Pack。
- 当前公告补证：回到完整ParsedDocument定向检索并补建证据。
- 跨地区/历史/政策扩展：GraphRAG召回，构建`revision_evidence_pack`后局部修订。

“新疆与山东挂网对比”可先召回1～3条高相关材料，但数量应由证据充分性决定，最多不超过辅助材料上限10条。

用户自助修订最多3轮；超过3轮或仍有阻断问题转人工。`user_revision_count`只在一轮修订成功产生新版本后递增。

## 第11步：偏好记忆候选与沉淀

用户反馈日志、审计记录和长期偏好记忆必须分开。

- 只抽取稳定样式偏好，不抽取公告事实、一次性任务要求和敏感数据。
- 候选状态：`pending`；用户明确同意后变为`approved/active`。
- 保存来源反馈ID、适用范围、版本、创建时间、过期时间和撤销状态。
- 用户可以查看、修改和删除。
- 记忆只影响风格，优先级低于系统规则、证据和当前指令。
- 不进入共享GraphRAG或公司知识事实库，不得跨客户复用。

## 跨步骤工程机制矩阵

| 机制 | 统一规则 |
|---|---|
| 幂等 | `run_id + node_name + input_hash`；任务创建使用`customer_id + idempotency_key`唯一约束 |
| 重试 | 仅网络、限流、短时存储故障重试；指数退避+抖动；不可恢复错误直接失败或人工 |
| 超时 | API、下载、OCR、解析、检索、模型、导出、节点和整任务分层超时 |
| Watchdog | 检查`heartbeat_at`、`current_node`和`watchdog_deadline_at`，恢复卡死任务或转人工 |
| Checkpoint | 每个成功节点保存输入/输出指纹和最后完成节点，重试从安全点继续 |
| 并发 | 队列隔离、模型信号量、附件并行、相同附件分布式锁、报告版本乐观锁 |
| 安全 | 鉴权、租户审计、资料范围过滤、文件沙箱、Prompt注入隔离、日志脱敏、短时下载授权 |
| 可观测性 | OpenTelemetry关联`run_id/trace_id`，记录排队、节点、检索、模型、存储、重试和成本 |
| 降级 | GraphRAG→关键词/手选；OCR/解析器切换；LLM压缩→规则压缩；DeepEval失败不阻塞 |

## 面试表述

> 这条链路不是简单的“RAG召回后让模型生成，再质检两次”。我先在FastAPI层完成鉴权、额度、幂等和可靠入队；主材料决定当前事实范围，GraphRAG只负责从共享公开库和公司审核库召回辅助候选，候选仍要解析成可追溯A/B证据。C级只保存辅助信息，推理结论进入独立Analysis Layer。完整Evidence Pack永久保留，24万字符只是应用硬上限，模型实际输入还要预留Prompt和输出Token；超限时规则压缩、LLM受约束压缩或分阶段生成。报告经过规则门和语义质检，最多自动修复两次；确定性质量结果同步展示，DeepEval异步旁路。用户反馈最多自助修订三轮，需要补证时重新构建revision evidence pack。长期记忆只保存经用户确认的稳定样式偏好，不能污染事实或跨客户共享。每个节点都有幂等、Checkpoint、超时、可恢复重试、并发池和OpenTelemetry Trace。

---

# 21. 确定性解析器是否只是关键词提取以及如何识别模糊语义

## 问题

确定性解析器是否就是发现“申报时间”“采购量”等关键词后截取附近文本？系统怎样判断某段内容已经超出规则能够稳定覆盖的范围，需要调用受约束LLM或转人工？

## 结论

确定性解析器不等于关键词提取。更准确地分为五层：

```text
文件结构解析
  -> 业务候选识别
  -> 字段语义映射
  -> 值标准化
  -> 一致性与约束校验
```

1. **文件结构解析**：`pypdf`、`python-docx`、`openpyxl`等读取页、段落、表格、单元格和位置。该层通常不理解“这是申报截止时间”，只负责恢复结构和来源定位。
2. **业务候选识别**：关键词、同义词词典、正则、表头别名和邻近窗口用于找到候选片段。关键词只是入口信号。
3. **字段语义映射**：根据标题、章节、表头、行列关系、公告类型和项目阶段，将候选映射到`submission_deadline`、`purchase_volume`等标准字段。
4. **值标准化**：把日期、金额、数量、百分比和枚举转为统一格式，同时保留原值和不确定性。
5. **一致性校验**：检查候选是否唯一、类型是否合法、是否违反跨字段约束、是否与其他来源冲突、来源位置是否有效。

“确定性”指相同输入、相同规则版本和相同配置得到相同输出，并且每一步可以解释和回放；不表示整个上游都绝对无误。OCR本身可能带置信度，因此OCR低置信文本需要作为风险信号。

## 清晰语义示例

Excel表头精确为“申报截止时间”，同一列单元格为“2026-08-10 17:00”，全表只有一个有效候选，日期解析成功，公告阶段为申报。系统可以直接生成高置信B级事实：

```json
{
  "field": "submission_deadline",
  "raw_value": "2026-08-10 17:00",
  "normalized_value": "2026-08-10T17:00:00+08:00",
  "derived_from": ["ev_a_cell_001"],
  "extraction_method": "exact_header_rule",
  "rule_confidence": 0.98,
  "ambiguity_flags": []
}
```

## 规则难以稳定覆盖的判断信号

系统不应只使用“正则没匹配到”作为判断，而应组合以下信号：

| 信号 | 含义 | 示例 |
|---|---|---|
| `multiple_candidates` | 同一字段存在多个合理候选 | 正文同时出现公告发布日期、报名开始日和申报截止日 |
| `unknown_or_multi_match_header` | 表头不在词典中，或可映射多个字段 | “时间要求”既可能表示开始时间，也可能表示截止时间 |
| `cross_sentence_dependency` | 含义依赖前后句或省略主语 | “上述期限顺延5个工作日”需要找到“上述期限” |
| `cross_document_dependency` | 需要关联补充公告、原公告或历史轮次 | “其他要求按原公告执行” |
| `incomplete_value` | 值缺少年份、单位、币种或时区 | “5月20日前”“单价不高于3.5” |
| `scope_unclear` | 不确定内容属于当前项目还是历史/辅助材料 | 历史中选价出现在对比章节 |
| `exception_or_modality` | 存在否定、例外、原则上、暂定等限定 | “原则上不超过两年，特殊情形除外” |
| `layout_ambiguity` | 合并单元格、跨页表格或错乱阅读顺序 | 一个表头覆盖两组数值列 |
| `low_source_confidence` | OCR或解析质量低 | OCR字符置信度低、关键数字疑似错识别 |
| `source_conflict` | 多个来源对同一字段给出不同值 | 正文截止日与附件截止日不一致 |
| `out_of_domain_pattern` | 文档表达偏离当前规则训练/标注分布 | 新公告模板或新业务术语 |

## 路由规则

不能只用一个平均分决定全部情况，应采用“硬条件 + 可校准分数”：

```text
无硬风险 + 候选唯一 + 类型/范围/来源校验通过 + 高规则置信度
  -> 直接生成B级事实

存在可解释的语义歧义，但原文位置清楚、风险可控
  -> 受约束LLM生成候选映射
  -> Schema、字段白名单、引用位置和确定性规则再次校验

高风险字段冲突、来源无法定位、OCR关键数字低置信或LLM结果无法验证
  -> 保留unknown/conflict
  -> 转人工复核
```

可以将阈值设计为可配置项，例如`rule_confidence >= 0.90`且没有硬风险时自动通过，`0.65～0.90`进入LLM候选路由，低于阈值或触发高风险硬条件时转人工；具体阈值必须用脱敏标注集校准，不能凭经验固定。

## LLM的正确职责

LLM不是替代解析器重新“读整份文件”，而是在受限上下文内解决候选之间的语义映射。例如输入表头、相邻行、候选列索引和公告类型，要求输出：

```json
{
  "candidate_field": "submission_deadline",
  "selected_source_indices": [3, 4],
  "reason_code": "deadline_phrase_with_context",
  "confidence": 0.82,
  "uncertainty": "表头仅写时间要求"
}
```

之后程序仍需检查索引是否存在、字段是否在白名单、值能否确定性解析、是否与其他证据冲突。LLM输出只能成为候选B级事实，不能直接成为正式证据。

## 如何证明规则稳定或不稳定

不能只看单份文档，应维护脱敏标注集，并按字段统计：

- Precision：规则提取出来的事实有多少正确；
- Recall：应提取的事实有多少被规则覆盖；
- 冲突率：同一字段产生多个候选的比例；
- `manual_review_rate`：进入人工复核的比例；
- `llm_fallback_rate`：进入LLM候选路由的比例；
- 模板外失败率：新模板或新术语导致的失败比例。

某条规则即使在示例上可运行，如果在不同地区、公告类型和附件复杂度分层中Precision或Recall波动很大，也属于“不稳定覆盖”，需要扩充规则、调整路由或使用LLM候选。

## 面试表述

> 确定性解析不是看到关键词就截一段文本。文件解析层先恢复页、段落、表格和单元格；业务提取层再用词典、正则、表头映射、上下文窗口和约束校验识别候选。只有候选唯一、来源清晰、类型和跨字段约束都通过时，才直接生成B级事实。多候选、未知表头、跨句引用、例外条款、OCR低置信和来源冲突会触发模糊语义标记。中等风险场景由受约束LLM只做候选映射，输出还要经过字段白名单、索引和来源校验；高风险冲突直接转人工。规则是否稳定最终通过分层标注集上的Precision、Recall和回退率证明，而不是凭关键词匹配是否成功判断。

---

# 22. 最终九步包装流程如何确定并转换成简历3+15

## 问题

用户将包装完成态重新整理为九步：主辅材料输入、两阶段证据包、四部分Prompt生成、第一次质检、定向修订、二次质检、后端校验与交付、用户反馈修订、偏好记忆沉淀。该流程如何与已有LangGraph、Celery、DeepEval和多级存储口径保持一致，并生成简短的3个项目描述和15条工作内容？

## 最终结论

九步流程作为后续简历和面试的**业务主叙事顺序**，已有工程能力作为贯穿机制保留，不重新设计另一条链路。

### 1. 数据输入

- 主材料：用户显式选择1～3个项目公告；
- 辅助材料：用户可手选0～10个；未手选时公司GraphRAG自动召回1～10个；
- GraphRAG支持关键词、向量和知识图谱检索，并进行权限、地区、时间、有效期过滤和Rerank；
- 上游公告平台负责每天约9000～10000条公告的采集，本项目只使用数据，不把该数字写成本项目产出。

### 2. 两阶段证据包

- 第一阶段构建完整Full Evidence Pack：MySQL HTML正文清洗；PDF使用`pypdf+OCR`；DOCX使用`python-docx`；XLSX/XLSM使用`openpyxl`做结构化摘要、有限样例和统计；失败使用Apache Tika兜底；
- Evidence由确定性解析器、领域规则、业务词典、正则和结构化提取器共同构建；复杂PDF和模糊表格可调用受约束LLM生成候选；
- A级保存原文及位置，B级保存A级的结构化表达和`derived_from`，C级保存摘要、生成指导和解析告警，不能单独支撑事实；
- 第二阶段依据字符和Token预算生成Compact Payload。

### 3. 24万字符调整说明

用户希望“超过24万字符时压缩到接近24万”。工程上调整为：

> 超过安全预算时触发规则或LLM压缩，将输入控制在24万字符硬上限和模型Token预算内；实际结果通常低于上限，并为Prompt、偏好、输出Token和安全余量预留空间。

调整原因：字符数不等于Token数，填满字符上限可能导致模型请求超限或挤占报告输出空间。

### 4. 生成、质检和修订

- Prompt固定为系统规则、用户任务、Compact Payload与引用、用户确认偏好四部分；
- 生成节点输出ReportIR、Markdown和Claim-Evidence引用；
- 第一次质检检查机构、时间、标的物、章节、数值日期及A/B证据支持；
- 修订节点只修改问题段落，并携带问题码和允许使用的A/B证据；
- 二次质检使用同一质量门；自动修复最多2次。

### 5. 后端校验与交付

- 后端检查非空、阻断、ReportIR/Markdown一致、对象哈希、版本关系和持久化状态；
- 前端展示二次质检问题、A/B支持率、无证据事实数量和可交付状态；
- 只有`deliverable=true`的版本允许下载Word；
- 超时、重试、降级、Watchdog和并发控制贯穿所有节点，不只在该步骤执行。

### 6. 用户反馈和记忆

- 预制按钮使用反馈意见、当前报告和原Evidence Pack进行修订，结果仍需质检；
- 自由文本分为样式/结构、当前公告补证、额外知识补充三类；
- 跨地区对比通常先由GraphRAG召回1～3个高相关文档，再构建`revision_evidence_pack`；
- 用户最多自助修订3轮，超过后提交人工；
- 每次反馈先进入日志，只将稳定样式偏好提取为候选记忆，用户确认后才加入长期Profile。

## 与工程包装的关系

- LangGraph：编排九步节点、条件边、State和Checkpoint；
- Celery/Redis：异步执行检索、解析、生成、质检、修订、导出和DeepEval；
- MySQL/MinIO/Redis：分别保存业务状态、大对象和短期队列缓存；
- DeepEval：离线回归和线上异步旁路，不替代同步双重质检；
- OpenTelemetry：串联API、队列、节点、模型和存储调用。

## 字段和指标解释

| 名称 | 中文含义 | 数据来源 | 为什么需要 | 读写节点 | 脱敏示例 |
|---|---|---|---|---|---|
| `application_input_cap_chars` | 应用输入字符硬上限 | 系统配置 | 触发安全压缩，防止输入超预算 | 压缩节点读取 | `240000` |
| `claim_ab_support_rate` | Claim获得A/B证据支持的比例 | 质量门计算 | 衡量事实追溯程度并辅助交付判断 | 质检写；前端读 | `0.96` |
| `unsupported_claim_count` | 无原文证据支持的事实数量 | 规则和语义质检 | 识别幻觉与证据遗漏 | 质检写；修订读 | `2` |
| `user_revision_count` | 用户自助修订轮数 | 成功生成新版本后累加 | 限制最多3轮并与网络重试区分 | 修订写；路由读 | `2` |
| `memory_candidate` | 待用户确认的偏好候选 | 反馈分析节点 | 防止一次性要求直接污染长期记忆 | 记忆节点写；确认接口读写 | `结论保持简洁` |

## 简历表达原则

- 项目描述保持一到两行；一份简历只选择3个候选描述中的1个；
- 工作内容准备15条素材，实际投递只放5～7条；
- 数字优先使用材料数量、24万字符、双重质检、最多2次自动修复和3轮用户修订等确定口径；
- Faithfulness、首轮通过率、无证据断言率和容量指标必须标注为模拟评测或目标容量；
- 不再使用“项目覆盖或采集每天9000～10000条公告”的表述。

## 面试表述

> 我把最终流程归纳为九个业务步骤：先确定1～3个主公告和辅助材料，辅助材料可手选，也可由公司GraphRAG通过关键词、向量和知识图谱自动召回；随后解析MySQL正文和多格式附件，构建完整A/B/C Evidence Pack，再按24万字符硬上限和Token预算生成Compact Payload。生成Prompt由系统规则、当前任务、证据上下文和用户确认偏好四部分组成，并要求Claim返回Evidence引用。报告经过第一次质检、局部修订和二次质检，后端再检查阻断、版本和持久化状态，决定是否允许Word交付。用户反馈按样式修改、当前公告补证和GraphRAG知识扩展三类路由，最多自助修订3轮；最后只将用户确认的稳定样式偏好沉淀为长期记忆。LangGraph、Celery、分层存储、幂等、Checkpoint、DeepEval和OpenTelemetry作为贯穿九步的工程能力。

---

## 更新记录

### 2026-08-04：第22题

- 将用户最新总结确定为九步最终业务包装流程。
- 修正公告数据归属：9000～10000条/日属于上游采集平台，本项目只读取和分析。
- 明确辅助材料手选0～10个、GraphRAG自动召回1～10个。
- 明确Apache Tika作为通用解析兜底。
- 将“压缩到接近24万”调整为“控制在24万字符硬上限与Token安全预算内”。
- 保留第一次质检、定向修订、二次质检和最终后端校验的职责边界。
- 明确预制按钮修订仍需原Evidence Pack和质量检查。
- 确认跨地区补充通常先召回1～3个高相关文档、用户最多自助修订3轮。
- 同步形成简历3个项目描述和15条工作内容。
- 将下一条问题编号更新为23。

### 2026-08-04：第21题

- 明确确定性解析器不等于关键词截取。
- 将Evidence提取拆分为文件结构解析、候选识别、字段映射、值标准化和一致性校验五层。
- 增加多候选、未知表头、跨句/跨文档依赖、值不完整、例外条款、布局歧义、OCR低置信和来源冲突等模糊语义信号。
- 确认采用硬条件与可校准置信度共同路由：规则直出、LLM候选或人工复核。
- 明确LLM输出只能成为待验证候选，不能直接成为正式B级事实。
- 将下一条问题编号更新为22。

### 2026-08-04：第20题

- 校正用户总结的完整数据流。
- 确认GraphRAG混合召回、主材料1～3条、辅助材料0～10条。
- 明确C级与Analysis Layer分离。
- 明确24万字符是硬上限，不是目标填充长度。
- 增加第0步任务创建和第11步偏好记忆沉淀。
- 确认自动修复最多2次、用户自助修订最多3轮。
- 增加跨步骤幂等、重试、超时、Watchdog、安全、并发、降级和可观测性机制。

### 2026-08-04：第19题

- 确定客户化后的日均与峰值分析任务容量口径。
- 保留内部每天10份以内作为历史事实。
- 客户化完成态采用日均300～500份、峰值约1000份/日目标容量。

### 2026-08-03

- 整理第1～18题并建立跨对话文档同步规则。
