# 8099 项目 S0-S5 最终结项决定

- 决定日期：2026-07-20
- 责任模式：单人项目，由仓库所有者统一承担开发、发布、验收和运行责任
- 结项状态：`OWNER_ACCEPTED_CLOSED_WITH_DOCUMENTED_WAIVERS`
- 下一阶段入口：以本结项变更最终进入 GitHub `main` 的完整 SHA 为唯一代码基线

## 1. 结项决定

仓库所有者确认，8099 项目的 S0-S5 功能实现、既有部署、前端衔接、运行手册和文档收口满足结束当前优化阶段并启动新计划的管理要求。

本决定把 S0-S5 标记为“项目阶段已关闭”，但不把没有执行过的测试、扫描或数据验收改写为已经执行。历史计划、运行手册和验收记录中列明的实际证据及缺口继续有效。

## 2. 集成基线

最终集成从 GitHub `main` 的以下提交开始：

```text
88309d57dcda056efa250cf37c2d77757536445e
```

结项集成纳入：

- `codex/pause-unsupported-fact-repair`：`bfb33fc1a3bede73bcaa8c0814117f713b6f0d02`
- `codex/s5-internal-acceptance`：`bb3659bd9d93845fecd7bd07f2dfcfe32633a7a5`
- `codex/readme-ai-engineer-20260720`：`2cb0bd1ed686b306c3d3198def7c14bd559cc5a4`

其中后端不受支持事实修订保持暂停；检测结果用于标记和人工复核，不恢复“检测后自动删除事实”的旧行为。

## 3. 历史验收边界

以下事实保持原样：

- S0 当时完成固定3和既有制品验证，但没有完成后来计划要求的第一次完整固定10。
- S2 当时属于条件验收，第二次完整固定10、完整 Word 扫描和完整 baseline compare 没有完成。
- 取消后续数据测试后的 S5 收口只执行了已批准的静态、文档、Git 和只读可用性检查；不能表述为严格 M2/M3 数据门禁已经执行。
- 本次结项不运行新的真实数据生成，不补写不存在的报告、Word、扫描或运行记录。

仓库所有者接受上述历史缺口对应的剩余风险，并决定不再以补跑旧阶段数据测试作为启动下一阶段计划的前置条件。

## 4. 已保留证据

- S5 运行手册、替代计划和批准记录保留在 Git 历史中。
- 当前线上修订暂停的设计、实施计划、配置和发布工具测试保留在 Git 历史中。
- 阶段唯一验收资料已集中归档为 `8099-stage-closure-archive-20260720.zip`。
- 归档 SHA-256：

```text
6273854DCD513F9987FDC7D3476174BC178CE0F0989264939A284AF1C9E621E1
```

## 5. 下一阶段约束

新计划必须：

1. 以结项后 GitHub `main` 的完整 SHA 为基线，不使用旧 worktree 或旧阶段分支作为开发起点。
2. 重新计算新变更的影响指纹，不能用本结项决定代替新阶段测试等级判断。
3. 保持 8099 与 8100 隔离，不操作 8100。
4. 不把 memory、history、checkpoint 或 diagnostics 当作报告事实来源。
5. 保持当前 Word 正文安全、EvidenceItem、compact、PDF/OCR 和正式载体边界，除非新计划明确批准变更。
6. 新阶段如发生生产代码变化，重新建立发布、备份、验证和回退证据。

## 6. 分支收口

本决定进入 `main` 并建立结项标签后，已被 `main` 完整包含的 S0-S5 功能分支可以删除。Git 提交历史和结项标签作为长期追溯入口。
